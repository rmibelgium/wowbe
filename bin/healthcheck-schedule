#!/bin/sh

# Use `pgrep` to check if `schedule:work` process is running
if pgrep -f "php artisan schedule:work" > /dev/null; then
  echo "\033[0;32mHealthcheck passed: Scheduler is running.\033[0m"
  exit 0
else
  echo "\033[0;31mHealthcheck failed: Scheduler is not running.\033[0m"
  exit 1
fi